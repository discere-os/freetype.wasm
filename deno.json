{
  "name": "@discere-os/freetype.wasm",
  "exports": {
    ".": "./",
    "./side": "./install/wasm/freetype-side.wasm",
    "./main": "./install/wasm/freetype-main.js"
  },
  "tasks": {
    "build:main": "meson setup build-main --cross-file=scripts/emscripten.cross -Ddefault_library=static -Dbuildtype=release --prefix=$PWD/install -Dlibdir=wasm -Dbindir=wasm && meson compile -C build-main freetype-main && meson install -C build-main",
    "build:side": "meson setup build-side --cross-file=scripts/emscripten.cross -Ddefault_library=static -Dbuildtype=release --prefix=$PWD/install -Dlibdir=wasm -Dbindir=wasm && meson compile -C build-side freetype-side && meson install -C build-side",
    "build:wasm": "deno task build:main && deno task build:side && deno task manifest",
    "manifest": "deno run --allow-read --allow-write --allow-run ../../../../scripts/generate-wasm-manifest.ts .",
    "bench": "deno run --allow-read bench/webcrypto-bench.ts",
    "clean": "rm -rf build-* install/"
  }
}
