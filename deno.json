{
  "name": "@discere-os/freetype.wasm",
  "exports": {
    ".": "./",
    "./side": "./install/wasm/freetype-side.wasm",
    "./main": "./install/wasm/freetype-main.js"
  },
  "tasks": {
    "build:main:meson": "meson setup build-main --cross-file=scripts/emscripten.cross -Dbuildtype=release --prefix=$PWD/install -Dlibdir=wasm -Dbindir=wasm && meson compile -C build-main freetype-main && meson install -C build-main",
    "build:side:meson": "meson setup build-side --cross-file=scripts/emscripten.cross -Dbuildtype=release --prefix=$PWD/install -Dlibdir=wasm -Dbindir=wasm -Dc_args='-msimd128' -Dc_link_args='-sSIDE_MODULE=2 -fPIC -O3 -flto' && meson compile -C build-side freetype-side && meson install -C build-side && python3 scripts/postinstall.py $PWD/install",
    "build:wasm:meson": "deno task build:main:meson && deno task build:side:meson"
  }
}
