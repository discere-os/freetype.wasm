{
  "name": "@discere-os/freetype.wasm",
  "exports": {
    ".": "./",
    "./side": "./install/wasm/freetype-side.wasm",
    "./main": "./install/wasm/freetype-main.js"
  },
  "tasks": {
    "build:main:meson": "meson setup build-main --cross-file=scripts/emscripten.cross -Ddefault_library=static -Dbuildtype=release --prefix=$PWD/install -Dlibdir=wasm -Dbindir=wasm && meson compile -C build-main freetype-main && meson install -C build-main",
    "build:side:meson": "meson setup build-side --cross-file=scripts/emscripten.cross -Ddefault_library=static -Dbuildtype=release --prefix=$PWD/install -Dlibdir=wasm -Dbindir=wasm && meson compile -C build-side freetype-side && meson install -C build-side",
    "build:wasm:meson": "deno task build:main:meson && deno task build:side:meson && deno task manifest",
    "manifest": "deno run --allow-read --allow-write --allow-run ../../../../scripts/generate-wasm-manifest.ts .",
    "clean": "rm -rf build-* install/"
  }
}
